import React from "react"

import "./Map.css"

export default function Map({ className, style }) {
   return (
      <div className={`map ${className}`} style={style}>
         Map
      </div>
   )
}
